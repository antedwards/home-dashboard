{
  "name": "home-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Smart family calendar and home dashboard with offline-first sync",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,js,svelte}\"",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "@turbo/gen": "^2.3.3",
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.8",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.14.4",
  "engines": {
    "node": ">=20.0.0"
  }
}
