name = "home-dashboard-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Pages build output
pages_build_output_dir = ".svelte-kit/cloudflare"

# Hyperdrive database connection
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "c4dc02b996ec44bb921033b4d434d9fe"

# Note: Replace REPLACE_WITH_YOUR_HYPERDRIVE_ID with your actual Hyperdrive ID from step 2.2
# You can find it by running: npx wrangler hyperdrive list
